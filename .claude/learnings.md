- In the `graph` parser, attach the `MATCH`-level `WHERE` inside `visitSimpleMatchStatement` rather than patching it in `parseMatchQuery`, otherwise `parseGQLStatement` and `parseMatchQuery` can diverge and drop the predicate.
- For `IAST`-based graph nodes, keep ownership in `children` and use raw pointer child references plus setters like `setOrReplace`; this makes `clone`, rewrites, and visitor bindings much more predictable.
- For build-native auto-formatting in ClickHouse, keep repo-wide `clang-format-all` for one-shot sweeps, but use target-scoped `CLANG_FORMAT_SETUP` for automatic formatting during normal builds.
- If `build/build.ninja` exists but `build/CMakeFiles/rules.ninja` is missing, treat the build directory as configure-incomplete; rerun `cmake` first, because `ninja` targets like `clang-format-all` cannot work on that directory.
- A target-scoped `clang-format` custom target without an output file is always dirty in Ninja; use a stamp-producing `add_custom_command` plus a wrapper target to make build-time formatting incremental.
- To make target-scoped `clang-format` incremental within a large target, keep a per-target stamp and have the runner format only files newer than that stamp unless `.clang-format`, the ignore file, or the manifest changed.
- After a repo-wide reformat, headers that relied on transitive standard-library includes can start failing in unrelated translation units; make headers like `futex.h` self-contained instead of depending on include order.
- After a repo-wide reformat, comments can accidentally get spliced into `#include` paths; scan for `#include` lines containing `//` inside the path when seemingly impossible header-not-found exceptions appear.
- In `graph` parser files that include `antlr4-cpp-runtime` headers directly, wrap those includes with local `#pragma clang diagnostic` suppressions; otherwise `clickhouse_parsers` can fail under `-Weverything -Werror` on upstream runtime documentation and destructor-override warnings even when `_gql_grammar` itself builds cleanly.
- When porting kgraph `INode` trees onto ClickHouse `IAST`, treat `IAST::children` as a dense non-null owner list; optional graph children should live in named fields and be pushed into `children` only when present, not represented as null indexed holes.
- For `graph` AST refactors, keep node classes under `src/Parsers/graph/AST/` with `GQL*` names and leave `GraphAST.h` as a thin umbrella include so callers can choose granular headers without breaking existing include sites.
- `ParserGraphQuery::parseImpl` currently gates the whole GQL entry on a leading `MATCH`, so top-level forms like `OPTIONAL MATCH` stay unreachable even though `GQLParseTreeVisitor` already handles optional match clauses.
- After the `graph` AST root refactor, the remaining hard build break is in tests that still include old headers like `ASTGraphQuery.h` and `GQLParsingUtil.h`; the new parser sources themselves can compile in isolation from the build directory.
- If `cmake` generation fails because `ninja -t recompact` segfaults before compilation starts, check stale `build/.ninja_deps`; moving it aside can recover both `cmake` regeneration and default `ninja`.
- In `graph` parser code, do not add a local fallback definition of `__lsan_ignore_object`; `base/base/phdr_cache.cpp` already provides one on Linux builds, so use a declaration plus guarded call to avoid duplicate-symbol link failures.
- For `graph` parser exceptions, use an existing error code like `SYNTAX_ERROR`; `ErrorCodes::CANNOT_PARSE_QUERY` is not defined and only shows up as a linker exception late in full builds.
- For the long-lived `graph` parser refactor, keep the live roadmap in `.claude/gql_refactor_status.md` so future sessions can recover the agreed scope and next steps quickly.
- In the `graph` visitor refactor, focused `USE` queries can be flattened by preserving `USE graphExpression` as a raw-text clause and appending the existing simple/result clauses, which expands coverage without introducing graph-selection AST yet.
- In the `GQL` visitor, `CALL` needs an explicit inline-vs-named shape bit; otherwise `CALL { ... }` gets reformatted like `CALL foo(...)` and picks up invalid parentheses.
- For top-level `SELECT FROM` work in the `GQL` visitor, a thin `source item` / `source list` wrapper is enough to preserve graph-qualified query sources and graph-match lists without forcing a deep relational source tree up front.
- Keep query roots aligned with the newer semantic contract: linear queries return `GQLSingleQuery`, set queries return `GQLCombinedQuery`, and nested procedure bodies stay wrapped in `GQLSubquery`.
- Preserve full set-operator semantics in `GQLCombinedQuery`: store explicit operator variants such as `UNION ALL` and `EXCEPT`, not just the operator family.
- Keep `SELECT` and `RETURN` as separate clause nodes (`GQLSelectClause`, `GQLReturnClause`) and keep paging in a dedicated `GQLPageClause` instead of patching `ORDER BY` / `OFFSET` / `LIMIT` back into the projection clause.
- For top-level focused queries in the `GQL` visitor, a thin `GQLUseClause` wrapper is enough to make `USE graphExpression` structured while still keeping the graph selector itself as a raw-text expression.
- `graphExpression` and `bindingTableExpression` do not need deep semantic lowering up front; thin `GQLGraphExpression` / `GQLBindingTableExpression` wrappers are enough to remove top-level raw-text payloads from `USE`, `SELECT FROM`, and binding initializers while deeper `graphReference` decomposition stays deferred.
- In `visitSelectStatement`, finish querying the `antlr4` `selectStatement` context before constructing the final clause node; constructing the clause too early can corrupt later parse-tree token lookups and trigger the old projection-path exception on `SELECT ... FROM ...`.
- To align `GQL` nested queries with `kgraph`, prefer a dedicated subquery wrapper node over ever-growing unwrap special cases; unsupported inner statement pieces can still fall back locally to raw text inside that wrapper.
- For query-level `GQL` cleanup, prefer explicit `Unsupported GQL ...` exceptions over `raw text` fallback when a nested procedure body or focused linear query shape cannot yet be represented; this keeps the query-root contract stable even before the deeper AST exists.
- For nested `GQL` procedure bodies, keep wrapper metadata such as `AT schema`, binding-definition blocks, and `NEXT YIELD` as dedicated `IAST` nodes inside `GQLSubquery` instead of flattening them into raw-text leaves.
- For `CALL` normalization, split named and inline procedure calls into distinct AST nodes; named calls should keep a full `GQLYieldClause`, while inline calls should point directly at `GQLSubquery`.
- In the current `GQL.g4`, `schemaReference` does not accept a bare schema identifier; use predefined or path-based forms in tests and model bare names only after the grammar changes.
- In `GQL.g4`, `inlineProcedureCall` is `variableScopeClause? nestedProcedureSpecification`; `yieldClause` belongs only to `namedProcedureCall`, so structured inline `CALL` support should reuse the nested procedure-body builder and reject variable scope until that AST exists.
- In this repo, `.claude/tools/cppexpr.sh` can add a temporary `add_subdirectory(cppexpr_...)` line to `src/CMakeLists.txt` plus helper directories while it is running; if you abort it, clean those artifacts before committing unrelated parser work.
- When creating local dev environments for this repo, prefer the official `ci/docker/binary-builder/Dockerfile` / `clickhouse/binary-builder` chain as the base if the current server setup already uses it, instead of inventing a separate generic image.
- On current `binary-builder`-derived `Ubuntu 22.04` environments, refresh the `Kitware` APT key before extra `apt-get update` runs; otherwise installing extra packages in derived images can fail before package resolution.
- `apt-get install antlr4` works on `Jammy`, but it installs `ANTLR` `4.7.2` under `/usr/share/java/antlr4.jar`, so `graph` grammar tooling should not assume only `/usr/local/lib/antlr-4.13.2-complete.jar`.
- If `graph` grammar generation must stay on `ANTLR` `4.13.2`, prefer downloading the exact complete jar into `/usr/local/lib` instead of relying on Ubuntu's older `antlr4` package.
- For `ClickHouse` devcontainers on macOS, keep the source tree bind-mounted but move the build directory onto a Docker named volume or another container-local path to avoid Docker Desktop bind-mount I/O bottlenecks.
- On macOS devcontainers, the cleanest compromise is often mounting the Docker volume directly at `/workspace/ClickHouse/build`, so editors keep the familiar path while build I/O stays off the host bind mount.
- For this repo, keep branch and PR targets module-scoped: use names like `parser/dev-...` for development branches, prefer module main branches like `parser/main-...`, and format commit messages as `[module] kind : summary`.
- GitHub rulesets can require PRs and block direct pushes on target branches, but they do not natively express "base branch X only accepts head branches matching Y"; if we choose not to add a repository workflow for that, the source-branch flow remains a convention only.
- In this personal fork, upstream `.github/workflows/*.yml` `Praktika` / self-hosted CI is dead weight without dedicated runners; removing the whole workflow set is cleaner than debugging permanently pending `Config Workflow` jobs.
- For this repo's long-lived branch sync, prefer `rebase` over merge when updating from the base branch; keep `amend` opt-in only.
- For `GQL` path coverage, keep classic `node` / `edge` / `path` nodes for relationship-local semantics, but lower `simplifiedPathPatternExpression` into a separate wrapper/expression pair; classic chain models like `Nebula` or `Kuzu` do not cover `GQL` path-expression operators cleanly.
- In the `GQL` grammar, `simplifiedOverrideAnyDirection` (`GQL.g4:1265-1266`) is a real `MINUS_SIGN simplifiedSecondary` branch, so `getSimplifiedOverrideDirection` should cover all 7 override alts explicitly and use `throwUnsupported(...)` for any future unknown branch instead of silently falling back to `EdgeDirection::Any`.
- In `GQLSimplifiedPathExpr::clone`, clear both `children` and `operands` after `make_intrusive<...>(*this)`, then rebuild `operand` / `quantifier` and `cloneChildrenList(...)`; otherwise the clone's intermediate `operands` vector can still point at source children and look like intentional sharing.
- Simplified-path round-trip preserves AST shape but normalizes whitespace around binary operators such as `|`, `|+|`, `&`, and concatenation gaps, so shape tests should assert structured nodes plus normalized `formatAST(...)` output rather than raw source spacing.
